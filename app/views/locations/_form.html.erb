<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :proximityUUID %><br>
    <%= f.text_field :proximityUUID %>
  </div>
  <div class="field">
    <%= f.label :UUID %><br>
    <%= f.text_field :UUID %>
  </div>
  <div class="field">
    <%= f.label :Trap %><br>
    <%= f.text_field :Trap %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
